# Logotherapy Booking System

Система бронювання сеансів у логотерапевта з повним функціоналом авторизації, календарем, матеріалами та управлінням записами.

## 📋 Структура проекту

```
logotherapy-booking/
├── index.html                 # Головна сторінка з бронюванням та матеріалами
├── bookings.html              # Сторінка перегляду заброньованих сеансів
├── package.json               # Залежності проекту
├── src/
│   ├── main.js                # Основний файл логіки головної сторінки
│   ├── bookings.js            # Логіка сторінки перегляду записів
│   ├── style.css              # Стилізація календаря та інтерфейсу
│   ├── data/
│   │   └── logotherapy-materials.json  # JSON з 200 матеріалами про логотерапію
│   └── modules/
│       ├── api.js             # API модуль (axios, кешування, цитати, матеріали)
│       ├── calendar.js        # Клас календаря для вибору дати
│       ├── storage.js         # Робота з localStorage (записи)
│       └── validation.js     # Валідація email та телефону
└── public/
    └── vite.svg
```

## 🚀 Запуск проекту

```bash
npm install
npm run dev
```

Проект буде доступний за адресою `http://localhost:5173`

## 🎯 Функціонал

### 1. Авторизація та реєстрація

**Файли:** `src/main.js`, `src/bookings.js`

- **Реєстрація:** Створення нового облікового запису з ім'ям, email та паролем
- **Вхід:** Авторизація за email та паролем
- **Вихід:** Завершення сесії користувача
- **Зберігання:** Дані користувача зберігаються в `localStorage` під ключем `logotherapy_user`
- **Нормалізація email:** Email автоматично переводиться в нижній регістр для уникнення проблем з реєстром

**Функції:**
- `loadCurrentUser()` - завантаження поточного користувача з localStorage
- `saveCurrentUser(user)` - збереження користувача в localStorage
- `updateAuthUI()` - оновлення інтерфейсу відповідно до стану авторизації

### 2. Календар та бронювання

**Файли:** `src/main.js`, `src/modules/calendar.js`

- **Календар:** Відображення місячного календаря з можливістю навігації
- **Вибір дати:** Клік по дню відкриває доступні часові слоти
- **Часові слоти:** 8 доступних слотів (09:00-17:00 з перервою на обід)
- **Блокування:** Минулі дні та зайняті слоти недоступні для вибору
- **Форма бронювання:** ПІБ та телефон (з валідацією)
- **Збереження:** Записи зберігаються в `localStorage` під ключем `logotherapy_appointments`

**Клас Calendar:**
- `constructor(renderCallback)` - ініціалізація з callback функцією
- `nextMonth()` - перехід до наступного місяця
- `prevMonth()` - перехід до попереднього місяця
- `render(containerId)` - відображення календаря в контейнері

**Функції:**
- `onDateSelected(dateStr)` - обробка вибору дати та відображення слотів

### 3. Валідація даних

**Файл:** `src/modules/validation.js`

**Валідація телефону:**
- Підтримка форматів: `+380XXXXXXXXX`, `0XXXXXXXXX`, `380XXXXXXXXX`
- Автоматична нормалізація до формату `+380XXXXXXXXX`
- Валідація в реальному часі з візуальним фідбеком

**Валідація email:**
- Перевірка формату через regex
- Перевірка локальної частини та домену
- Дотримання RFC 5321 (максимальна довжина 254 символи)
- Валідація в реальному часі

**Функції:**
- `validatePhone(phone)` - перевірка телефону
- `validateEmail(email)` - перевірка email
- `normalizePhone(phone)` - нормалізація телефону до стандартного формату

### 4. Матеріали з логотерапії

**Файли:** `src/modules/api.js`, `src/data/logotherapy-materials.json`

- **JSON база:** 200 матеріалів про логотерапію в окремому JSON файлі
- **Бесконечний скролл:** Автоматична підгрузка матеріалів при прокрутці
- **Кешування:** Матеріали кешуються в `sessionStorage` на 1 годину
- **Пагінація:** По 10 матеріалів на сторінку

**Функції:**
- `fetchArticlesPage(page, limit)` - завантаження сторінки матеріалів
- `loadArticlesPage()` - завантаження наступної сторінки
- `setupArticlesInfiniteScroll()` - налаштування бесконечного скролу

### 5. Цитата дня

**Файл:** `src/modules/api.js`

- **API:** Використання `quotable.io` для отримання випадкових цитат
- **Кешування:** Цитата кешується в `localStorage` на 1 годину
- **Fallback:** При помилці API використовується резервна цитата

**Функції:**
- `fetchDailyQuote()` - завантаження цитати з кешуванням

### 6. Сторінка перегляду записів

**Файли:** `bookings.html`, `src/bookings.js`

- **Перегляд:** Відображення всіх заброньованих сеансів користувача
- **Пошук:** Пошук за датою, часом, ім'ям або телефоном
- **Фільтрація:** Фільтр за статусом (всі / майбутні / минулі)
- **Сортування:** Майбутні записи спочатку, потім за датою
- **Статуси:** Візуальне відображення статусу запису (майбутній / минулий)

**Функції:**
- `loadAppointments()` - завантаження записів користувача
- `filterAppointments()` - фільтрація та пошук записів
- `renderAppointments()` - відображення записів у вигляді карток
- `formatDate(dateStr)` - форматування дати українською мовою
- `getStatus(appointment)` - визначення статусу запису

### 7. Робота з даними (localStorage)

**Файл:** `src/modules/storage.js`

**Ключі localStorage:**
- `logotherapy_user` - дані поточного користувача
- `logotherapy_appointments` - масив всіх записів
- `logotherapy_quote_cache` - кеш цитати дня
- `logotherapy_articles_cache_*` - кеш матеріалів (sessionStorage)

**Функції:**
- `getAppointments()` - отримання всіх записів
- `saveAppointment(appointment)` - збереження нового запису
- `isSlotTaken(dateStr, timeStr)` - перевірка, чи зайнятий слот

## 🔧 Технології

- **Vite** - збірщик та dev-сервер
- **Bootstrap 5** - UI фреймворк
- **Bootstrap Icons** - іконки
- **Axios** - HTTP клієнт для API запитів
- **Vanilla JavaScript** - без фреймворків

## 📦 Залежності

```json
{
  "axios": "^1.6.0",
  "vite": "^7.2.4"
}
```

## 🎨 Особливості UI/UX

- **Адаптивний дизайн:** Працює на всіх пристроях
- **Валідація в реальному часі:** Миттєвий фідбек при введенні даних
- **Візуальні індикатори:** Кольорові індикатори валідності полів
- **Модальні вікна:** Bootstrap модалки для авторизації та підтвердження
- **Бесконечний скролл:** Плавна підгрузка матеріалів
- **Кешування:** Оптимізація швидкості через кешування даних

## 🔐 Безпека

- Паролі зберігаються як base64 (для демо-проекту)
- Email нормалізується для уникнення проблем з реєстром
- Валідація всіх вхідних даних на клієнті
- Записи прив'язані до email користувача

## 📝 Формати даних

**Користувач:**
```javascript
{
  name: "Ім'я Користувача",
  email: "user@example.com",
  passwordHash: "base64_encoded_password"
}
```

**Запис:**
```javascript
{
  id: "uuid",
  userEmail: "user@example.com",
  name: "ПІБ клієнта",
  phone: "+380123456789",
  date: "2024-12-25",
  time: "14:00",
  createdAt: "2024-12-20T10:30:00.000Z"
}
```

## 🌐 API

**Зовнішні API:**
- `https://api.quotable.io/random?tags=wisdom` - цитати дня

**Внутрішні дані:**
- `src/data/logotherapy-materials.json` - матеріали про логотерапію

## 🐛 Відомі обмеження

- Паролі зберігаються в base64 (не для продакшну)
- Один користувач на сесію (не підтримка багатьох акаунтів одночасно)
- Дані зберігаються тільки в localStorage (немає бекенду)
- Немає можливості видалення або редагування записів

## 📄 Ліцензія

Проект створений для навчальних цілей.

