# Logotherapy Booking System

Система бронювання сеансів у логотерапевта з авторизацією, календарем, розділом матеріалів та керуванням записами. Після останнього рефакторингу логіка розбита на дрібні модулі, тому підтримувати та розвивати застосунок стало простіше.

## 📋 Структура проекту

```
logotherapy-booking/
├── index.html                 # Головна сторінка з бронюванням та матеріалами
├── bookings.html              # Сторінка перегляду заброньованих сеансів
├── package.json               # Залежності проекту
├── src/
│   ├── main.js                # Точка входу: ініціалізація модулів
│   ├── bookings.js            # Логіка сторінки перегляду записів
│   ├── style.css              # Стилізація календаря та інтерфейсу
│   ├── data/
│   │   └── logotherapy-materials.json  # 200 матеріалів про логотерапію
│   └── modules/
│       ├── api.js             # HTTP-запити та кешування даних
│       ├── articles.js        # Нескінченний скрол матеріалів
│       ├── auth.js            # Модальні форми реєстрації/логіну
│       ├── booking.js         # Робота з формою бронювання та списком сесій
│       ├── calendar.js        # Клас календаря
│       ├── quote.js           # Завантаження цитати дня
│       ├── session.js         # Стан поточного користувача та UI
│       ├── storage.js         # Робота з localStorage/sessionStorage
│       └── validation.js      # Перевірка email та телефону
└── public/
    └── vite.svg
```

## 🚀 Запуск проекту

```bash
npm install
npm run dev
```

Проект буде доступний за адресою `http://localhost:5173`

## 🎯 Функціонал

### 1. Авторизація та сесія користувача

**Файли:** `src/modules/auth.js`, `src/modules/session.js`, `src/main.js`

- **Реєстрація/вхід/вихід:** Модальні форми Bootstrap із живою валідацією email.
- **Сесія:** `session.js` нормалізує email, зберігає користувача під ключем `logotherapy_current_session` та оновлює стан UI.
- **База користувачів:** Зберігається в `localStorage` під ключем `logotherapy_users_db`.

**Основні функції:**
- `initSession()`, `getCurrentUser()`, `saveCurrentUser()` — керування поточним користувачем.
- `updateAuthUI()` — показує/ховає елементи в залежності від авторизації.
- `setupAuthHandlers()` — реєстрація, логін, лог-аут та їх побічні ефекти (перевалідація форми, оновлення списків).

### 2. Календар та бронювання

**Файли:** `src/modules/calendar.js`, `src/modules/booking.js`

- **Календар:** Клас `Calendar` відповідає за відображення місяця, навігацію та вибір дати.
- **Слоти:** `booking.js` генерує 8 часових слотів (09:00-17:00, з перервою) та блокує зайняті.
- **Форма:** Валідується у реальному часі, кнопка активується лише після вибору часу, валідних даних та входу в систему.
- **Збереження:** Усі записи потрапляють у `localStorage` (`logotherapy_appointments`).

**Основні функції:**
- `initBooking()` — підключає всю ланку бронювання та повертає колбеки (`onDateSelected`, `revalidateBooking`, `handleUserChange`, `renderUpcomingAppointments`).
- `renderUpcomingAppointments()` — список найближчих 5 сесій користувача.

### 3. Валідація даних

**Файл:** `src/modules/validation.js`

- Підтримка форматів телефону `+380XXXXXXXXX`, `0XXXXXXXXX`, `380XXXXXXXXX` та автоматична нормалізація.
- Строга перевірка email (довжина, формат, крапки, дефіси).
- Використовується як у формах модалок, так і в формі бронювання.

**Функції:** `validatePhone`, `normalizePhone`, `validateEmail`.

### 4. Матеріали з логотерапії

**Файли:** `src/modules/articles.js`, `src/modules/api.js`, `src/data/logotherapy-materials.json`

- JSON-база на 200 записів.
- `fetchArticlesPage()` реалізує пагінацію та кеш у `sessionStorage` (`logotherapy_articles_cache_*`).
- `setupArticlesInfiniteScroll()` слухає прокрутку контейнера та викликає `loadArticlesPage()` для підвантаження наступних 10 матеріалів.

### 5. Цитата дня

**Файли:** `src/modules/quote.js`, `src/modules/api.js`

- `loadDailyQuote()` викликає `fetchDailyQuote()` із кешем `logotherapy_quote_cache`.
- Якщо зовнішній API недоступний — показується резервний текст.

### 6. Сторінка перегляду записів

**Файли:** `bookings.html`, `src/bookings.js`

- Сортування, пошук, фільтрація (всі/майбутні/минулі) і візуальні статуси записів.
- Працює виключно з локальними даними користувача.

### 7. Робота з даними (storage)

**Файл:** `src/modules/storage.js`

**Ключі:**
- `logotherapy_appointments` — масив записів.
- `logotherapy_users_db` — зареєстровані користувачі.
- `logotherapy_current_session` — поточна сесія.
- `logotherapy_quote_cache` — кеш цитати.
- `logotherapy_articles_cache_*` — кеш сторінок матеріалів у `sessionStorage`.

**Функції:** `getAppointments`, `saveAppointment`, `isSlotTaken`, `getAllUsers`, `saveNewUser`, `getSessionUser`, `setSessionUser`.

## 🔧 Технології

- **Vite** — dev-сервер та збірник
- **Bootstrap 5 + Icons** — UI та іконки
- **Axios** — HTTP клієнт
- **Vanilla JavaScript** — без фреймворків

## 📦 Залежності

```json
{
  "axios": "^1.6.0",
  "vite": "^7.2.4"
}
```

## 🎨 Особливості UI/UX

- Адаптивний дизайн
- Валідація в реальному часі
- Інтерактивні індикатори стану полів
- Bootstrap-модалки для авторизації та підтверджень
- Нескінченний скролл матеріалів
- Кешування клієнтських даних

## 🔐 Безпека

- Паролі зберігаються у форматі base64 (демо-рівень)
- Email нормалізується перед зберіганням/пошуком
- Валідація клієнтських даних
- Записи прив’язані до email користувача

## 📝 Формати даних

**Користувач:**
```javascript
{
  name: "Ім'я Користувача",
  email: "user@example.com",
  passwordHash: "base64_encoded_password"
}
```

**Запис:**
```javascript
{
  id: "uuid",
  userEmail: "user@example.com",
  name: "ПІБ клієнта",
  phone: "+380123456789",
  date: "2024-12-25",
  time: "14:00",
  createdAt: "2024-12-20T10:30:00.000Z"
}
```

## 🌐 API

- `https://api.quotable.io/random?tags=wisdom` — цитати дня
- Локальні дані: `src/data/logotherapy-materials.json`

## 🐛 Відомі обмеження

- Паролі не хешуються криптографічно
- Один активний користувач на пристрій
- Дані зберігаються лише в браузері (без бекенду)
- Немає редагування/видалення записів

## 📄 Ліцензія

Проект створений для навчальних цілей.

